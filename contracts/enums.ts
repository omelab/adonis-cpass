//enums fields for post status
export enum PostStatus {
  DRAFT = 'Draft',
  PUBLIC = 'Public',
  PRIVATE = 'Private',
  DELETED = 'Deleted',
}

//enum fields for basic status
export enum StatusEnum {
  ACTIVE = 'Active',
  INACTIVE = 'Inactive',
  DELETED = 'Deleted',
}
