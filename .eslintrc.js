module.exports = {
  extends: [
    // other extensions
    'prettier',
    // other extensions
  ],
  plugins: [
    // other plugins
    'prettier',
    // other plugins
  ],
  rules: {
    // other rules
    'prettier/prettier': ['error', { endOfLine: 'auto' }],
    // other rules
    'parser': '@babel/eslint-parser',
  },
}
